// Minimal managed entry point required by the browser-wasm SDK.
// The real entry points are the [JSExport] methods in Converter.cs.
// Nothing to run here — the JS side drives everything.
return 0;
